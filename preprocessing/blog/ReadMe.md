# Blog

1) First run clean_blog.py
2) Downsample the blog data, which splits the data into training, dev and test.
3) Create Author Profiles for each classifier and the feature pipeline with with create_author_profiles. create_author_profiles.py takes in
   the downsampled data of downsample_blog.py as input.