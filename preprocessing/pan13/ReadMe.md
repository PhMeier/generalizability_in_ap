# PAN 2013
1) Read in the dataset and create training and test partition
2) Run html_parsing.py to parse the html data and clean the data
3) Downsample the data (Only training partition)
4) Create profiles.